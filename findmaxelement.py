def find_max_element():
    N = int(input())  # Read the length of the array
    arr = list(map(int, input().split()))  # Read the array elements
    print(max(arr))  # Print the maximum element

find_max_element()
